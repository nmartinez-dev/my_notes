const Header = () => {
  return (
    <div
      style={{
        fontSize: '2rem',
        color: '#000',
        textAlign: 'center',
        padding: '1.5rem',
        fontWeight: 'bold',
      }}>
      Mis Notas
    </div>
  );
};

export default Header;
